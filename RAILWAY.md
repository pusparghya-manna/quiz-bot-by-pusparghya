# Railway Deploy Instructions

1. Service Settings → Root Directory = `/backend`
2. Build Command = `npm install && npm run build`
3. Start Command = `node dist/index.js`
4. Add all environment variables listed in README
