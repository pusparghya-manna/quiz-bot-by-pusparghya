PORT=8080
APP_PRODUCTION=true
# Production fails boot without JWT_SECRET (>=24) and TELEGRAM_WEBHOOK_SECRET (>=16)
DATABASE_URL=jdbc:postgresql://HOST:5432/quizbot
DATABASE_USERNAME=quizbot
DATABASE_PASSWORD=
JWT_SECRET=
ALLOWED_ORIGINS=https://quiz-bot-by-pusparghya.vercel.app
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=   # required when APP_PRODUCTION=true
# Production fails boot without JWT_SECRET (>=24) and TELEGRAM_WEBHOOK_SECRET (>=16)
TELEGRAM_POLLING_ENABLED=true
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest
MAX_OCR_BASE64_CHARS=10000000
