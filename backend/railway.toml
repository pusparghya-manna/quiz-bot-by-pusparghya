[build]
builder = "RAILPACK"
buildCommand = "npm install && npm run build && test -f dist/index.js"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/ready"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
