# Legacy Node backend (DEPRECATED)

This **Node.js + Express + Turso** stack is **not** the production backend.

**Production:** `backend-spring/` (Spring Boot + PostgreSQL). See root `README.md` and `ENTERPRISE.md`.

Kept only for reference and one-time migration to Postgres (`scripts/migrate-turso-to-postgres.md`).

Do not point Railway at this directory for new deployments.
